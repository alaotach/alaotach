name: Update readme.md

on:
  push:
  schedule:
    - cron: "0 */1 * * *"

permissions:
  contents: write

jobs:
  markscribe:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master

      - uses: 'actions-go/go-install@main'
        id: install
        env:
          GOPROXY: "direct"
          GOSUMDB: "off"
        with:
          module: "github.com/cherrytree56567/markscribe@master"

      - name: generate readme
        env:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          WAKATIME_URL: "https://hackatime.hackclub.com/api/v1"
        run: |
          echo -n "${{ secrets.TESTING }}" | sha256sum
          ${{ steps.install.outputs.install-path }} -write README.md README.md.tpl

      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Update generated README
          branch: main
          commit_user_name: readme-scribe 🤖 
          commit_user_email: actions@github.com
          commit_author: readme-scribe 🤖 <actions@github.com>
